<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext liberado so pro emulador (10.0.2.2) e pro IP da maquina
         local na rede (backend de dev rodando via uvicorn) — staging/prod
         continuam HTTPS-only, sem excecao, coerente com o checklist de
         producao (Secao 6.5). 192.168.1.11 e o IP atual da maquina de dev;
         troque aqui se ele mudar. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">192.168.1.11</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
