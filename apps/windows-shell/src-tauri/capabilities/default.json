{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissoes minimas do shell Windows (Plano Mestre v1.1, Secao 8.6): so o plugin HTTP (pra falar com o backend cloud contornando o problema de cookie cross-origin do webview), deep-link e store (sessao local).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "http://localhost:8000/*" },
        { "url": "http://127.0.0.1:8000/*" }
      ]
    },
    "deep-link:default",
    "store:default"
  ]
}
